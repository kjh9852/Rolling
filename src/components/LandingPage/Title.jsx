import styled from 'styled-components';

const Title = styled.h2`
  margin-top: 16px;
  margin-bottom: 8px;
  font-weight: 700;
  font-size: 24px;
  line-height: 36px;
  letter-spacing: -0.01em;
  color: var(--gray900);
`;

export default Title;
